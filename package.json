{
  "name": "ai-debate-arena",
  "version": "1.0.0",
  "description": "AI models debate each other to find the best answer",
  "main": "main.js",
  "scripts": {
    "dev": "concurrently \"vite --port 5173\" \"wait-on http://localhost:5173 && VITE_PORT=5173 electron .\"",
    "build": "vite build",
    "start": "electron .",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "concurrently": "^8.2.0",
    "electron": "^29.0.0",
    "vite": "^5.1.0",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
